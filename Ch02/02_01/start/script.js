// JavaScript Document

// Define a variable with an integer value
var myNumber = 5;
// assign = takes value on R and assigns it to thing on L 
//only 'number' type in js. ints floats etc. all handled by 'number'

// Send the value of the variable to the output console
console.log(myNumber);
//when you call a method/function you use (), if you wish to pass a value, goes inside ()
console.log(typeof myNumber);